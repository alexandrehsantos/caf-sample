name: caf_flutter_test
description: Test project for CAF Document Detector with middleware integration
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <4.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_caf_document_detector:
    path: ../document_detector
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true 